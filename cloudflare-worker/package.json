{
  "name": "password-rotator",
  "version": "1.0.0",
  "description": "Cloudflare Worker for rotating Firebase password and sending emails",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
